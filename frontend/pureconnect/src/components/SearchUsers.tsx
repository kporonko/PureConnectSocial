import React, { useState } from 'react';

interface SearchUsersProps {
    theme: string;
}

const SearchUsers: React.FC<SearchUsersProps> = ({ theme }) => {

    return (
        <div className="">
            Search
        </div>
    );
};

export default SearchUsers;